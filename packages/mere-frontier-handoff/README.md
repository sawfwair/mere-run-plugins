# Frontier Handoff

`mere-frontier-handoff` runs a bounded request through a user's signed-in Claude Code or Codex CLI. It stores the response in a private local result file and records its provenance without copying CLI credentials. It does not include application-specific prompts or policy.

See [the plugin guide](../../docs/plugins/frontier-handoff.md) for the request format and commands.
