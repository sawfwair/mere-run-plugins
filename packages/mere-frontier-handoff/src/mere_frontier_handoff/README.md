# Frontier handoff implementation

This package validates generic requests, records a local plan, invokes a user-authenticated frontier CLI, and writes a private result receipt. Provider output remains a candidate for the caller to validate.
