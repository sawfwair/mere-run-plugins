# SearXNG Search

`mere-searxng-search` queries a user-configured SearXNG instance through its JSON search API. It does not run a SearXNG server or install search engines. See the [plugin guide](../../docs/plugins/searxng-search.md).
