# Archive investigation example

`investigation.result.json` illustrates the result contract, including an
unresolved claim and optional metrics. Its facts and timing values are fictional;
it isn't an acceptance result. Validate measured behavior with the installed-model
test described in `packages/mere-archive-tools/ACCEPTANCE.md`.
